[phases.setup]
nixPkgs = ["python311", "sqlite", "gcc", "pkg-config"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r requirements.txt",
  "python -c 'import sqlite3; print(\"SQLite version:\", sqlite3.sqlite_version)'"
]

[phases.build]
cmds = ["echo 'Build completo'"]

[start]
cmd = "bash start.sh"